control_id: PCI-2.1
name: Default credentials removed
risk: >
  Default vendor passwords on system components create trivial entry points
  for attackers targeting cardholder data environments.
expected_state:
  default_credentials_removed: true
  vendor_supplied_defaults_changed: true
evidence_sources:
  - system: cloud_iam
    description: IAM policy audit for default/well-known credential patterns (mocked).
severity: high
check_frequency: daily
